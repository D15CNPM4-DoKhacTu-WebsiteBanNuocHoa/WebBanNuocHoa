@using WEB_NUOCHOA_TQ.Models
@model IEnumerable<SanPham>


<!--<link rel="stylesheet" type="text/css" href="/Link/css/bootstrap.css" />-->
<!-- font awesome style -->
@*<link href="/Link/css/font-awesome.min.css" rel="stylesheet" />*@
<!-- Custom styles for this template -->
<link href="/Link/css/style.css" rel="stylesheet" />
<!-- responsive style -->
@*<link href="/Link/css/responsive.css" rel="stylesheet" />*@
<header class="header_section" style="margin-left: 20%">
    <div class="container" style="height: 20px">
        <nav class="navbar navbar-expand-lg custom_nav-container ">
           
            <div class="collapse navbar-collapse" id="navbarSupportedContent" style="margin-left: 25%;margin-top: -30px">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">Trang Chủ <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <span class="nav-label">Sản Phẩm @*<span class="caret">*@</span></span></a>
                        <ul class="dropdown-menu">
                            @foreach (var groupItem in Model.GroupBy(n => n.TheLoai))
                            {
                                if (groupItem.Key != null)
                                {
                                    <li><a href="@Url.Action("SanPham", "SanPham", new {@IDLoaiSP = groupItem.Key.ID})">@groupItem.Key.Ten</a></li>
                                }

                            }
                        </ul>
                    </li>
                   
                    <li class="nav-item">
                        <a class="nav-link" href="#">Giới Thiệu</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Liên Hệ</a>
                    </li>

                </ul>
            </div>
        </nav>
    </div>
</header>
